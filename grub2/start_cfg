insmod  part_apple
insmod  part_bsd
insmod  part_gpt
insmod  part_msdos
insmod	normal
insmod	test
search --no-floppy --set=root -fs-uuid '%GRUB_UUID%'
if [ "${grub_platform}" == "efi" ]
    set efi = 1
else
    set bios = 1
    set efi = 0
fi
source ($root)/boot/grub/grub.cfg
